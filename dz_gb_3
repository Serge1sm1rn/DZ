#include <locale.h>
#include <iostream>

using namespace std;

/*
extern int a; 
extern int b;         // Объявляем переменные из файла 0.срр
extern int c;
extern int d;
*/

int main()
{
	setlocale(LC_ALL, "Russian");


	/*
		 int a;
		 int b;
	     int c;
		 int d;
		float f;
		float e;

		cout << "Введите переменную a: ";

		cin >> a; //Ввод переменной а

		cout << "Введите переменную b: ";                 //Задание №1

		cin >> b; //Ввод переменной b

		cout << "Введите переменную c: ";

		cin >> c; //Ввод переменной c

		cout << "Введите переменную d: ";

		cin >> d; //Ввод переменной d
		

		e = (float)c / d;

		f = a * (b + e);


		cout << "ответ: " << f;
		///////////////////////////////////////////////////////////////////////////
	*/     
	    //////////////////////////////////////////////////////////////////////////
	/*
	      int g;
	      int h;
	      cout << "Введите число h : ";
	      cin >> h;

	      g = (h <= 21) ? h - 21 : (h - 21) * 2;        //Задание №2

	      cout <<"Ответ: " g;
        ////////////////////////////////////////////////////////////////////////
		*/
	    ///////////////////////////////////////////////////////////////////////
	    /*
	     float f; 
	     float e;

	     e = (float)c / d;
		                                 //Задание №4
	     f = a * (b + e);


	     cout << "ответ: " << f;
		 */
		 /////////////////////////////////////////////////////////////////////

	     ////////////////////////////////////////////////////////////////////
	     /*
	     int array [3][3][3];

	     int* parr = nullptr;
		                          // Задание №3 (Скорее всего не правильно, потому что не может быть все так просто , но я старался))
	     parr = &array[1][1][1];
	
	     cout << parr;

	     */
	     /////////////////////////////////////////////////////////////////////

	}	

                                         КОНЕЦ ПЕРВОГО ФАЙЛА.
                                         
    #include <iostream>


using namespace std;


int a = 5;                                 переменные для 4-го задания
int b = 6;
int c = 7;
int d = 8;                                     
                                       КОНЕЦ ВТОРОГО ФАЙЛА
	

